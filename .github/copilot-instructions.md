# Copilot Instructions

- Use the `myenv` virtual environment for all Python execution.
- Activate with myenv/Scripts/activate (Windows).
- Use `uv` for package management and running scripts whenever possible.
- To run a script: `uv run --python myenv path/to/script.py` or activate the environment.
